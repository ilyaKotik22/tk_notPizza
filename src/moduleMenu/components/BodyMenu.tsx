import React from "react";

interface Props{
    ClassName?: string
}

export const BodyMenu: React.FC<Props> = ({ ClassName }) => {
    return (
        <div className={ ClassName}>
            
        </div>);
};